server:
  port: 8082

spring:
  application:
    name: notification
  profiles:
    active: dev
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka